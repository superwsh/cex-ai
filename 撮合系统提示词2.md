# CEX 撮合系统第二阶段 AI 编码提示词

## 一、你的角色

你是一名资深 CEX 交易所后端架构师和 Java 高性能系统工程师。

当前项目已经完成撮合系统第一阶段，已经具备基础订单簿和撮合能力。

你接下来的任务是：

**在不破坏第一阶段已有撮合逻辑的前提下，把撮合系统升级为一个具备 Kafka 顺序消费、WAL、Snapshot、宕机恢复、幂等、成交事件输出能力的可恢复撮合引擎。**

本阶段核心目标不是增加更多订单类型，而是解决：

* 消息有序性
* 撮合确定性
* 数据可靠性
* 宕机恢复
* Kafka 重复消费
* 撮合结果可靠输出
* 系统可测试性

---

# 二、第二阶段总体目标

完整链路设计为：

```text
Order Service
     │
     │ Kafka
     ▼
matching.command
     │
     ▼
Matching Consumer
     │
     ▼
Sequence Check
     │
     ▼
WAL Append
     │
     ▼
Matching Engine
     │
     ├── OrderBook
     │
     ├── Match
     │
     └── Generate Trade
     │
     ▼
Matching Result Event
     │
     ▼
Kafka
     │
     ├── matching.trade
     └── matching.order-result
```

撮合系统启动时：

```text
Matching Engine Start
        │
        ▼
Load Latest Snapshot
        │
        ▼
Restore OrderBook
        │
        ▼
Get lastSequence
        │
        ▼
Replay WAL
        │
        ▼
Restore latest state
        │
        ▼
Start Kafka Consumer
```

要求：

```text
Snapshot + WAL + 相同输入事件
```

无论重放多少次，都必须得到：

```text
相同 OrderBook
相同 Trade
相同 OrderStatus
相同 Sequence
```

也就是：

**撮合过程必须是 Deterministic 的。**

---

# 三、技术栈约束

项目使用：

```text
Java 17
Spring Boot 3.x
Kafka
MySQL
Redis（本阶段不是核心依赖）
Jackson
Maven
JUnit 5
```

撮合核心模块禁止依赖数据库完成实时撮合。

撮合核心状态：

```text
OrderBook
Order
PriceLevel
Sequence
```

主要保存在：

```text
Memory
```

持久化依赖：

```text
WAL
Snapshot
Kafka
```

---

# 四、模块划分

在现有 matching-service 中按照以下方式拆分。

```text
matching-service
│
├── application
│   ├── MatchingCommandHandler
│   ├── MatchingRecoveryService
│   └── SnapshotService
│
├── domain
│   ├── engine
│   │   └── MatchingEngine
│   │
│   ├── orderbook
│   │   ├── OrderBook
│   │   ├── PriceLevel
│   │   └── BookOrder
│   │
│   ├── model
│   │   ├── Side
│   │   ├── OrderType
│   │   ├── MatchingCommand
│   │   ├── Trade
│   │   └── MatchResult
│   │
│   └── sequence
│       └── SequenceManager
│
├── infrastructure
│   ├── kafka
│   │   ├── MatchingCommandConsumer
│   │   ├── MatchingEventProducer
│   │   └── KafkaConfig
│   │
│   ├── wal
│   │   ├── WalWriter
│   │   ├── WalReader
│   │   ├── WalRecord
│   │   └── WalManager
│   │
│   └── snapshot
│       ├── SnapshotManager
│       ├── SnapshotWriter
│       ├── SnapshotReader
│       └── MatchingSnapshot
│
└── interfaces
    └── health
```

禁止把：

```text
Kafka
WAL
Snapshot
MatchingEngine
```

全部堆在一个 Service 类中。

---

# 五、Kafka Topic 设计

## 1. matching.command

用途：

订单系统发送撮合命令。

Topic：

```text
matching.command
```

Partition Key：

```text
symbol
```

例如：

```text
BTC-USDT
ETH-USDT
```

要求：

同一个 symbol 的所有：

```text
NEW_ORDER
CANCEL_ORDER
```

必须进入同一个 Partition。

通过：

```text
symbol
```

作为 Kafka Key。

---

# 六、MatchingCommand 数据结构

统一定义：

```java
public class MatchingCommand {

    private long sequence;

    private String commandId;

    private String orderId;

    private String userId;

    private String symbol;

    private CommandType commandType;

    private Side side;

    private OrderType orderType;

    private long price;

    private long quantity;

    private long timestamp;
}
```

CommandType：

```java
NEW_ORDER
CANCEL_ORDER
```

不要在撮合核心中使用：

```java
double
```

价格和数量统一使用整数表示。

例如：

BTC 价格：

```text
65000.12 USDT
```

如果 priceScale = 2：

```text
6500012
```

数量同理。

---

# 七、Sequence 机制

每个 symbol 必须维护：

```text
lastProcessedSequence
```

例如：

```text
BTC-USDT
lastProcessedSequence = 1000234
```

收到消息：

```text
sequence = 1000235
```

允许处理。

收到：

```text
sequence <= lastProcessedSequence
```

视为：

```text
重复消息
```

直接忽略。

如果：

```text
sequence > lastProcessedSequence + 1
```

视为：

```text
sequence gap
```

不允许继续撮合。

系统必须：

```text
停止该 symbol 消费
记录 ERROR
触发恢复逻辑
```

不要跳过 sequence gap。

SequenceManager：

```java
public interface SequenceManager {

    long current(String symbol);

    void validate(String symbol, long sequence);

    void advance(String symbol, long sequence);
}
```

---

# 八、正确处理顺序

收到 Kafka command 后必须按照严格顺序处理：

```text
1 Validate Sequence
        ↓
2 Append WAL
        ↓
3 WAL Flush
        ↓
4 Matching
        ↓
5 Update lastProcessedSequence
        ↓
6 Produce Result Event
        ↓
7 Commit Kafka Offset
```

禁止：

```text
先撮合
再写 WAL
```

否则：

```text
撮合完成
进程宕机
WAL 没写
```

重启后无法恢复。

---

# 九、WAL 设计

WAL：

```text
Write Ahead Log
```

核心原则：

**任何改变 OrderBook 状态的命令，都必须先写 WAL。**

例如：

```text
NEW_ORDER
CANCEL_ORDER
```

---

# 十、WalRecord

定义：

```java
public class WalRecord {

    private long sequence;

    private String commandId;

    private String orderId;

    private String userId;

    private String symbol;

    private CommandType commandType;

    private Side side;

    private OrderType orderType;

    private long price;

    private long quantity;

    private long timestamp;

    private long checksum;
}
```

要求：

每一条 WAL Record 都必须支持：

```text
checksum
```

用于检测：

```text
文件损坏
partial write
```

---

# 十一、WAL 文件

建议：

```text
/data/matching/wal/
```

目录：

```text
BTC-USDT/
    wal-000001.log
    wal-000002.log

ETH-USDT/
    wal-000001.log
```

单文件达到指定大小后滚动，例如：

```text
256MB
```

生成：

```text
wal-000002.log
```

要求配置化：

```yaml
matching:
  wal:
    enabled: true
    path: /data/matching/wal
    max-file-size: 268435456
```

---

# 十二、WAL Flush

第一版优先可靠性。

每次：

```text
append
```

之后执行：

```text
force/fsync
```

保证 WAL 已写入磁盘。

接口：

```java
public interface WalWriter {

    void append(WalRecord record);

    void flush();
}
```

后续性能优化阶段再考虑：

```text
batch fsync
group commit
```

当前阶段不要过早优化。

---

# 十三、Snapshot 设计

Snapshot 用于解决：

```text
WAL 太长
重启重放时间过长
```

Snapshot 必须保存：

```text
symbol
lastSequence
snapshotTimestamp
OrderBook
```

结构：

```java
public class MatchingSnapshot {

    private String symbol;

    private long lastSequence;

    private long snapshotTimestamp;

    private List<SnapshotOrder> buyOrders;

    private List<SnapshotOrder> sellOrders;
}
```

SnapshotOrder 至少保存：

```text
orderId
userId
side
price
remainingQuantity
sequence
timestamp
```

---

# 十四、Snapshot 文件

目录：

```text
/data/matching/snapshot/
```

例如：

```text
BTC-USDT/
    snapshot-100000.bin
    snapshot-200000.bin
```

Snapshot 文件名包含：

```text
lastSequence
```

方便恢复时选择最新快照。

---

# 十五、Snapshot 写入必须原子化

禁止直接写：

```text
snapshot-200000.bin
```

必须：

```text
snapshot-200000.tmp
```

完整写入完成：

```text
fsync
```

然后：

```text
atomic rename
```

变成：

```text
snapshot-200000.bin
```

避免宕机产生损坏快照。

---

# 十六、Snapshot 触发策略

支持配置：

```yaml
matching:
  snapshot:
    enabled: true
    interval-seconds: 60
    command-count: 100000
```

可以根据：

```text
时间
```

或者：

```text
处理命令数量
```

触发。

第一版建议：

```text
每 100000 条 command
```

创建一次 Snapshot。

---

# 十七、Recovery 流程

实现：

```java
MatchingRecoveryService
```

启动时执行。

恢复算法：

```text
1 查找 symbol 最新有效 Snapshot

2 校验 Snapshot

3 Load Snapshot

4 Restore OrderBook

5 获取 snapshot.lastSequence

6 查找所有 sequence > lastSequence 的 WAL

7 按 sequence 顺序 replay

8 Restore MatchingEngine

9 更新 lastProcessedSequence

10 启动 Kafka Consumer
```

---

# 十八、WAL Replay

Replay 时：

禁止：

```text
再次写 WAL
```

否则会出现：

```text
WAL -> replay -> 再写 WAL
```

无限增长。

提供：

```java
matchingEngine.execute(command, ExecutionMode.REPLAY);
```

ExecutionMode：

```java
LIVE
REPLAY
```

LIVE：

```text
正常撮合
产生外部 Kafka Event
```

REPLAY：

```text
恢复 OrderBook
不重复写 WAL
```

对于 Trade 是否再次发送 Kafka：

默认：

```text
不发送
```

因为 Replay 的目标是：

```text
恢复内部状态
```

而不是重复通知下游。

---

# 十九、OrderBook 快照一致性

创建 Snapshot 时必须保证：

```text
snapshot.sequence
```

和：

```text
OrderBook 状态
```

完全对应。

由于每个 symbol 单线程撮合，因此推荐：

在撮合线程安全点执行：

```text
snapshot
```

禁止另一个线程直接遍历正在修改的：

```text
TreeMap
Queue
```

---

# 二十、成交 TradeId 设计

TradeId 必须确定性生成。

禁止使用：

```java
UUID.randomUUID()
```

否则 Replay 时会产生不同 TradeId。

推荐：

```text
symbol + takerOrderId + makerOrderId + matchSequence
```

例如：

```text
BTC-USDT-10001-90001-1000234
```

或者使用 Snowflake，但 Replay 时必须能够恢复原始 TradeId。

推荐第一版直接：

```text
hash(
    symbol +
    takerOrderId +
    makerOrderId +
    sequence +
    matchIndex
)
```

其中：

```text
matchIndex
```

表示同一 command 中第几个成交。

例如：

```text
sequence=100
matchIndex=0
matchIndex=1
matchIndex=2
```

---

# 二十一、撮合输出事件

定义：

```text
matching.trade
```

保存：

```text
TradeCreatedEvent
```

结构：

```java
public class TradeCreatedEvent {

    private String tradeId;

    private long sequence;

    private String symbol;

    private String makerOrderId;

    private String takerOrderId;

    private String makerUserId;

    private String takerUserId;

    private Side takerSide;

    private long price;

    private long quantity;

    private long timestamp;
}
```

---

# 二十二、Order Result Topic

定义：

```text
matching.order-result
```

事件类型：

```text
ORDER_PARTIALLY_FILLED

ORDER_FILLED

ORDER_CANCELLED

ORDER_REJECTED
```

结构：

```java
public class OrderResultEvent {

    private String orderId;

    private String symbol;

    private long sequence;

    private OrderResultType type;

    private long filledQuantity;

    private long remainingQuantity;

    private long timestamp;
}
```

---

# 二十三、Kafka Producer

实现：

```java
MatchingEventProducer
```

至少：

```java
sendTradeEvent()

sendOrderResultEvent()
```

Kafka Key：

Trade：

```text
symbol
```

OrderResult：

建议：

```text
orderId
```

或者如果后续订单更新也要求严格 symbol 顺序，则：

```text
symbol
```

第一版统一：

```text
symbol
```

保证同一 symbol 的状态变化顺序。

---

# 二十四、Kafka Consumer

实现：

```java
MatchingCommandConsumer
```

要求：

关闭：

```text
enable.auto.commit
```

使用：

```text
manual ack
```

流程：

```text
receive command

try

    commandHandler.handle(command)

    acknowledge kafka offset

catch exception

    不提交 offset

    记录 error

    根据异常类型决定 pause partition
```

---

# 二十五、Partition 与 Engine

一个 Kafka Partition：

```text
只能由一个线程顺序处理
```

同一 symbol：

```text
只能进入一个 MatchingEngine
```

维护：

```java
Map<String, MatchingEngine>
```

例如：

```text
BTC-USDT -> MatchingEngine
ETH-USDT -> MatchingEngine
```

禁止：

```text
多个线程同时修改同一个 OrderBook
```

不要给：

```text
TreeMap
PriceLevel
OrderBook
```

加大量锁解决线程安全。

核心思路是：

```text
Single Writer Principle
```

---

# 二十六、Kafka Rebalance

需要考虑：

```text
Consumer Rebalance
```

Partition revoke 时：

至少执行：

```text
停止处理新命令
完成当前命令
flush WAL
记录 lastSequence
```

如果实现 Snapshot 成本不高，可以：

```text
创建 Snapshot
```

再释放 Partition。

---

# 二十七、宕机场景设计

必须实现并测试以下场景。

场景 1：

```text
Kafka 收到消息
WAL 未写
进程宕机
```

结果：

```text
Kafka Offset 未提交
重启重新消费
```

---

场景 2：

```text
WAL 已写
撮合未执行
进程宕机
```

结果：

```text
重启通过 WAL Replay 执行
```

---

场景 3：

```text
WAL 已写
撮合完成
Kafka Offset 未提交
宕机
```

重启：

```text
Kafka 重复消息
```

通过：

```text
sequence <= lastProcessedSequence
```

忽略。

---

场景 4：

```text
Snapshot 写到一半宕机
```

结果：

```text
.tmp 文件被忽略
加载上一个完整 Snapshot
```

---

场景 5：

```text
WAL 最后一条只写了一半
```

结果：

```text
checksum 校验失败
忽略 incomplete tail
```

不得导致整个系统无法启动。

---

# 二十八、日志

关键日志必须包含：

```text
symbol
sequence
orderId
commandId
tradeId
partition
offset
```

禁止只打印：

```text
matching failed
```

需要：

```text
Matching command failed:
symbol={}
sequence={}
orderId={}
partition={}
offset={}
```

---

# 二十九、Metrics

至少预留：

```text
matching_commands_total

matching_trades_total

matching_latency

wal_append_latency

snapshot_duration

snapshot_sequence

last_processed_sequence

replay_commands_total

recovery_duration

sequence_gap_total
```

可以使用：

```text
Micrometer
```

---

# 三十、测试要求

必须增加以下测试。

## 单元测试

```text
SequenceManagerTest

WalWriterTest

WalReaderTest

SnapshotWriterTest

SnapshotReaderTest

MatchingRecoveryServiceTest
```

---

## Replay 测试

创建：

```text
10000 条随机 Order Command
```

执行：

```text
Engine A
```

获得：

```text
OrderBook A
Trades A
```

然后：

```text
Snapshot + WAL
```

创建：

```text
Engine B
```

执行 Recovery。

要求：

```text
OrderBook A == OrderBook B
```

必须完全一致。

---

# 三十一、Deterministic Test

这是整个第二阶段最重要的测试。

输入：

```text
command1
command2
command3
...
command100000
```

分别运行：

```text
MatchingEngine A
MatchingEngine B
```

要求：

```text
Trade 数量相同

TradeId 相同

成交价格相同

成交数量相同

OrderBook 相同

lastSequence 相同
```

禁止使用任何会导致非确定性的：

```text
random
UUID
System.currentTimeMillis
HashMap 无序遍历结果
```

参与核心业务计算。

timestamp 必须来源于：

```text
MatchingCommand.timestamp
```

---

# 三十二、性能约束

不要为了第二阶段增加大量：

```text
synchronized
Lock
ConcurrentHashMap
```

撮合核心继续遵循：

```text
single thread per partition
single writer per OrderBook
```

允许：

```text
Kafka IO
Snapshot IO
Monitoring
```

在其他线程执行。

但是：

OrderBook 状态读取必须保证：

```text
一致性
```

---

# 三十三、明确禁止事项

禁止：

```text
MatchingEngine 内直接访问 MySQL
```

禁止：

```text
MatchingEngine 内直接修改 Account Balance
```

禁止：

```text
MatchingEngine 调用 Clearing Service RPC
```

禁止：

```text
随机生成 TradeId
```

禁止：

```text
使用 double 表示价格和数量
```

禁止：

```text
Kafka 自动提交 offset
```

禁止：

```text
多个线程同时修改同一个 OrderBook
```

禁止：

```text
先撮合再写 WAL
```

禁止：

```text
Recovery 时重复向 Kafka 发送历史 Trade
```

---

# 三十四、开发顺序

严格按照以下顺序实施。

第一步：

```text
统一 MatchingCommand
加入 sequence
```

第二步：

```text
实现 SequenceManager
```

第三步：

```text
实现 WAL 数据结构
WalWriter
WalReader
```

第四步：

```text
接入 MatchingCommandHandler

Sequence Check
↓
WAL
↓
Matching
↓
Sequence Advance
```

第五步：

```text
实现 Snapshot
```

第六步：

```text
实现 Recovery
```

第七步：

```text
接入 Kafka Consumer
```

第八步：

```text
实现 Trade / OrderResult Event
```

第九步：

```text
接入 Kafka Producer
```

第十步：

```text
实现宕机恢复测试
```

第十一步：

```text
实现 Deterministic Replay Test
```

第十二步：

```text
Metrics
Health Check
日志完善
```

不要跳步骤。

---

# 三十五、你开始编码前必须先做的事情

不要立即修改代码。

首先扫描当前项目。

输出：

```text
1 当前 matching-service 目录结构

2 当前 MatchingEngine 实现

3 当前 OrderBook 实现

4 当前 Order / Trade 数据模型

5 当前 Kafka 实现情况

6 当前线程模型

7 与本设计存在冲突的地方

8 可以复用的代码

9 需要新增的代码

10 第二阶段最终实施计划
```

然后再开始修改。

原则：

```text
优先复用现有代码
避免无意义重构
避免修改已经验证正确的撮合算法
```

---

# 三十六、每一步开发完成后的要求

每完成一个步骤：

必须输出：

```text
本步骤完成内容

修改文件列表

新增文件列表

关键设计说明

可能风险

测试结果

下一步准备做什么
```

如果测试失败：

```text
先解决测试
```

禁止带着失败测试进入下一阶段。

---

# 三十七、第二阶段最终验收标准

只有全部满足以下条件，第二阶段才能视为完成：

* Kafka 按 symbol 保证撮合命令有序
* 每条命令具有严格 sequence
* 重复消息不会重复撮合
* sequence gap 可以发现
* command 修改状态前先写 WAL
* WAL 支持 checksum
* WAL 支持文件滚动
* Snapshot 可以持久化完整 OrderBook
* Snapshot 保存 lastSequence
* Snapshot 使用 tmp + atomic rename
* Engine 可以从 Snapshot 恢复
* Snapshot 后的 WAL 可以 Replay
* Replay 不会重复写 WAL
* Replay 不会重复发送历史 Kafka Event
* TradeId 是确定性的
* Kafka offset 使用 manual commit
* OrderBook 继续使用 Single Writer
* kill -9 后系统可以恢复
* incomplete WAL 不会导致系统无法启动
* 损坏 Snapshot 可以回退旧 Snapshot
* Deterministic Test 通过
* Recovery 后 OrderBook 与宕机前一致
* 核心单元测试全部通过

---

# 三十八、第二阶段完成后的系统边界

第二阶段完成后，系统结构应该达到：

```text
             Order Service
                   │
                   ▼
          matching.command
                   │
                   ▼
        ┌──────────────────┐
        │ Matching Service │
        │                  │
        │ Kafka Consumer   │
        │       ↓          │
        │ Sequence         │
        │       ↓          │
        │ WAL              │
        │       ↓          │
        │ Matching Engine  │
        │       ↓          │
        │ OrderBook        │
        │                  │
        │ Snapshot         │
        │ Recovery         │
        └────────┬─────────┘
                 │
          ┌──────┴───────┐
          ▼              ▼
   matching.trade   matching.order-result
          │
          ▼
       下一阶段
   Clearing Service
```

本阶段不要开发：

```text
资金扣减
资金解冻
手续费
账户余额
资产流水
清算结算
行情 K 线
WebSocket 推送
```

这些属于后续阶段。

本阶段唯一目标：

**让撮合引擎从“能撮合”升级成“不会因为宕机、Kafka 重复消息或进程重启而丢失或破坏撮合状态”的可靠撮合系统。**
