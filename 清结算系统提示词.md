# CEX 清算结算系统开发提示词

你现在是一名资深数字货币交易所后端架构师和 Java 高级工程师。

当前正在开发一个中心化交易所 CEX。

目前系统已经完成：

* 用户基础体系
* 资产账户基础能力
* 订单系统
* 资金冻结
* Kafka 消息基础设施
* Outbox 可靠消息机制
* 撮合系统
* OrderBook
* Price-Time Priority
* 成交 Trade 生成
* 撮合结果 Kafka 推送

现在需要继续开发：

# Clearing & Settlement System

# 清算结算系统

本阶段目标不是简单修改用户余额，而是建立完整可靠的：

**Trade → Clearing → Settlement → Balance → Ledger → Outbox → Reconciliation → Compensation**

资金链路。

整个设计必须以金融系统标准考虑：

* 资金安全
* 原子性
* 幂等性
* 可审计
* 可追溯
* 最终一致性
* 故障恢复
* 重复消息
* 消息乱序
* 服务重启
* Kafka 重平衡
* 数据库事务失败
* 死锁
* 部分成交
* 多次成交
* 手续费
* 对账
* 补偿

禁止只实现一个简单的：

```text
UPDATE account SET balance = balance + xxx
```

清算结算系统必须按照下面的要求进行完整设计和开发。

---

# 一、业务背景

当前交易流程：

```text
Client
   ↓
Gateway
   ↓
Order Service
   ↓
资金冻结
   ↓
Order Created
   ↓
Kafka
   ↓
Matching Engine
   ↓
OrderBook
   ↓
Trade Created
   ↓
Kafka
   ↓
Clearing & Settlement System
```

清算结算完成后：

```text
Trade
 ↓
清算
 ↓
资产结算
 ↓
资金流水
 ↓
订单成交结果更新
 ↓
Outbox Event
 ↓
Kafka
 ├── Order Service
 ├── Market Data
 ├── Notification
 ├── Reconciliation
 └── Risk System
```

本系统只负责撮合成功之后的资产变化以及相关账务处理。

撮合引擎禁止直接修改用户资产。

---

# 二、首先明确 Clearing 与 Settlement 的职责

必须将两个概念在代码和架构中区分。

## Clearing 清算

负责根据 Trade 计算：

```text
Buyer 应支付多少 Quote Asset
Buyer 应获得多少 Base Asset

Seller 应支付多少 Base Asset
Seller 应获得多少 Quote Asset

Buyer Fee
Seller Fee

实际成交金额
实际解冻金额
资金变化
```

例如：

```text
BTC/USDT

price = 100000 USDT
quantity = 0.1 BTC

tradeAmount = 10000 USDT
```

买方：

```text
USDT frozen -10000
BTC available +0.1
```

卖方：

```text
BTC frozen -0.1
USDT available +10000
```

再分别处理 Buyer Fee 和 Seller Fee。

---

## Settlement 结算

负责真正执行：

```text
账户余额修改
冻结余额扣减
可用余额增加
手续费扣减
平台手续费账户增加
Ledger 记录
Settlement 状态修改
Outbox Event
```

所有核心账务动作必须处于可靠的数据库事务中。

---

# 三、现阶段系统定位

第一阶段优先实现：

# Database Based Settlement

暂时不要一开始就开发极端复杂的内存账本系统。

架构：

```text
Kafka Trade Event
      ↓
Settlement Consumer
      ↓
Idempotency Check
      ↓
Clearing Calculator
      ↓
Settlement Transaction
      ↓
Account Balance
      ↓
Ledger
      ↓
Settlement Record
      ↓
Outbox
      ↓
Commit
```

后续预留升级方向：

```text
Account Actor
Memory Ledger
Account Sharding
WAL
Snapshot
Event Sourcing
Distributed Ledger
```

但第一阶段以：

```text
MySQL Transaction
+
Kafka At-Least-Once
+
Database Unique Index
+
Outbox
+
Reconciliation
```

保证正确性。

---

# 四、核心原则

整个系统必须遵守以下原则。

## 1. 资金安全 > 性能

优先级：

```text
Correctness
>
Consistency
>
Auditability
>
Availability
>
Performance
```

任何性能优化不得破坏资金正确性。

---

# 五、Kafka 消费模型

撮合系统产生：

```text
trade.executed
```

事件。

推荐 Topic：

```text
cex.trade.executed
```

TradeEvent 至少包含：

```json
{
  "eventId": "xxx",
  "tradeId": "T123",
  "symbol": "BTC-USDT",

  "buyOrderId": "B001",
  "sellOrderId": "S001",

  "buyerUserId": 10001,
  "sellerUserId": 10002,

  "price": "100000",
  "quantity": "0.1",
  "quoteAmount": "10000",

  "buyerFee": "0.0001",
  "buyerFeeAsset": "BTC",

  "sellerFee": "10",
  "sellerFeeAsset": "USDT",

  "buyOrderFinished": false,
  "sellOrderFinished": true,

  "matchSequence": 1000231,
  "tradeTime": 1234567890
}
```

金额字段禁止使用：

```text
double
float
```

统一使用：

```text
BigDecimal
```

数据库统一使用：

```text
DECIMAL
```

精度根据资产精度统一配置。

---

# 六、Kafka 消费语义

不要假设 Kafka Exactly Once 就可以解决资金幂等问题。

系统按照：

```text
Kafka At-Least-Once
+
业务幂等
```

进行设计。

意味着同一个：

```text
tradeId
```

可能被消费：

```text
1 次
2 次
10 次
```

最终只能产生一次资金影响。

---

# 七、Trade 结算幂等设计

必须使用数据库唯一索引作为最终幂等防线。

设计：

```text
settlement_task
```

核心唯一约束：

```sql
UNIQUE KEY uk_trade_id (trade_id)
```

处理逻辑：

```text
收到 TradeEvent
        ↓
尝试创建 settlement_task
        ↓
创建成功
    → 当前消费者负责处理

tradeId 已存在
        ↓
查询 Settlement 状态
        ↓
SUCCESS
    → 直接 ACK

PROCESSING
    → 判断是否超时

FAILED / RETRY
    → 根据策略重新执行
```

禁止仅仅依赖：

```text
Redis SETNX
```

完成金融幂等。

Redis 可以作为性能优化，但数据库必须具有最终唯一约束。

---

# 八、数据库表设计

根据现有项目结构调整，不要无意义重复创建已经存在的资金表。

至少检查并设计以下实体。

---

## 8.1 account_balance

如果当前项目已经存在资产账户表，复用现有表。

逻辑字段：

```text
id
user_id
asset

available
frozen

version

created_at
updated_at
```

唯一约束：

```text
UNIQUE(user_id, asset)
```

金额类型：

```text
DECIMAL(36,18)
```

或者根据系统统一 Money Precision 规范设置。

要求始终满足：

```text
available >= 0
frozen >= 0
```

正常业务绝对禁止形成负余额。

---

# 九、Settlement Task

设计：

```text
settlement_task
```

建议字段：

```text
id

trade_id
event_id
symbol

buy_order_id
sell_order_id

buyer_user_id
seller_user_id

base_asset
quote_asset

price
quantity
quote_amount

buyer_fee
buyer_fee_asset

seller_fee
seller_fee_asset

status

retry_count
error_code
error_message

match_sequence

created_at
processing_at
settled_at
updated_at
```

状态：

```text
INIT
PROCESSING
SUCCESS
RETRY
FAILED
MANUAL_REVIEW
```

唯一索引：

```text
trade_id UNIQUE
```

必要时：

```text
event_id UNIQUE
```

---

# 十、资金流水设计

必须建立不可随意修改的资金流水：

```text
balance_flow
```

资金流水用于：

* 用户资产查询
* 资金审计
* 对账
* 故障排查
* 数据恢复
* 资金证明
* 补偿分析

建议字段：

```text
id
flow_id

biz_id
biz_type

trade_id
order_id

user_id
asset

account_type

available_before
available_change
available_after

frozen_before
frozen_change
frozen_after

direction

created_at
```

biz_type 示例：

```text
ORDER_FREEZE
ORDER_UNFREEZE

TRADE_BUY
TRADE_SELL

TRADE_FEE

DEPOSIT
WITHDRAW
REFUND
COMPENSATION
```

推荐唯一索引：

```text
trade_id
+
user_id
+
asset
+
biz_type
```

需要注意：

同一 Trade 同一用户可能同时产生多种资金变化。

不要设计一个错误的唯一键导致合法流水无法插入。

---

# 十一、推荐进一步引入 Journal

如果项目允许，增加：

```text
journal
journal_entry
```

形成类似复式记账的数据结构。

例如：

```text
Journal
----------------
journal_id
biz_type
biz_id
trade_id
status
created_at
```

Entry：

```text
journal_entry
----------------
journal_id
user_id
asset
account
amount
direction
```

可以定义内部账户：

```text
USER_AVAILABLE
USER_FROZEN
PLATFORM_FEE
SYSTEM_CLEARING
```

目标：

一个业务 Trade 的所有账务变化都能够通过 Journal 完整重建。

---

# 十二、现货交易结算逻辑

例如：

```text
BTC/USDT

Buyer
Seller

price = 100000
quantity = 0.1

quoteAmount = 10000
```

---

## Buyer

买方原来下单时已经冻结：

```text
USDT frozen
```

成交后：

```text
Buyer.USDT.frozen -= quoteAmount

Buyer.BTC.available += quantity
```

如果买方手续费使用 BTC：

```text
Buyer.BTC.available += quantity - buyerFee
Platform.BTC.available += buyerFee
```

---

## Seller

卖方之前已经冻结：

```text
BTC frozen
```

成交：

```text
Seller.BTC.frozen -= quantity

Seller.USDT.available += quoteAmount
```

如果手续费使用 USDT：

```text
Seller.USDT.available += quoteAmount - sellerFee
Platform.USDT.available += sellerFee
```

---

# 十三、Maker / Taker Fee

设计独立：

```text
FeeCalculator
```

禁止在 SettlementService 中到处出现：

```java
if (maker) {
...
}

if (vipLevel == ...) {
...
}
```

定义：

```text
FeeService

calculateMakerFee()
calculateTakerFee()
```

输入：

```text
userId
symbol
tradeAmount
quantity
maker/taker
VIP Level
fee configuration
```

输出：

```text
feeAmount
feeAsset
feeRate
```

TradeEvent 如果已经固定了手续费数据：

Settlement 原则上使用 TradeEvent 中已经确定的 fee。

不要由于用户 VIP Level 后续发生变化重新计算历史成交手续费。

---

# 十四、部分成交

必须完整支持：

```text
PARTIALLY_FILLED
```

例如：

用户挂单：

```text
BUY

1 BTC
price = 100000
```

冻结：

```text
100000 USDT
```

第一次：

```text
成交 0.2
```

结算：

```text
frozen USDT -20000
available BTC +0.2
```

仍然保留：

```text
frozen USDT = 80000
```

第二次：

```text
成交 0.3
```

继续扣除。

直到：

```text
FILLED
```

剩余冻结余额才根据订单实际成交情况处理。

---

# 十五、限价买单 Price Improvement

必须处理一个非常重要的场景。

用户限价：

```text
BUY BTC
Limit Price = 100000
Quantity = 1
```

创建订单时冻结：

```text
100000 USDT
```

实际成交：

```text
Trade Price = 99000
```

实际消费：

```text
99000 USDT
```

因此最终必须解冻：

```text
1000 USDT
```

但不要在每次 Trade 时盲目把价差全部解冻。

需要结合：

```text
order remainingQuantity
remainingFrozen
order status
```

设计正确的释放机制。

原则：

```text
部分成交
→ 保留后续成交所需冻结资金

完全成交
→ 释放剩余冻结资金

取消订单
→ 释放全部剩余冻结资金
```

---

# 十六、订单与资金职责边界

明确：

```text
Order Service
```

负责：

```text
订单生命周期
订单状态
订单剩余数量
```

```text
Matching Engine
```

负责：

```text
订单簿
撮合
Trade
```

```text
Settlement
```

负责：

```text
Trade 资产结算
手续费
账务流水
资金结果
```

不要让三个服务互相直接修改对方数据库。

跨服务状态通过：

```text
Kafka Event
```

同步。

---

# 十七、订单更新事件

Settlement 完成后使用 Outbox 产生：

```text
trade.settled
```

建议：

```text
cex.trade.settled
```

Event：

```json
{
  "tradeId": "T123",
  "symbol": "BTC-USDT",

  "buyOrderId": "B001",
  "sellOrderId": "S001",

  "buyerUserId": 10001,
  "sellerUserId": 10002,

  "price": "100000",
  "quantity": "0.1",

  "buyerFee": "0.0001",
  "buyerFeeAsset": "BTC",

  "sellerFee": "10",
  "sellerFeeAsset": "USDT",

  "settlementStatus": "SUCCESS",

  "settledAt": 123456789
}
```

Order Service 消费事件后：

```text
更新 filledQuantity
更新 remainingQuantity
更新 order status
```

订单更新同样需要：

```text
tradeId
```

进行幂等处理。

---

# 十八、Outbox

Settlement 成功后，禁止：

```text
DB commit
↓
Kafka send
```

简单串联。

否则可能发生：

```text
DB commit 成功
Kafka send 失败
```

最终下游永远不知道这笔交易已经完成结算。

必须使用：

```text
Transactional Outbox
```

事务中：

```text
BEGIN

账户修改
资金流水
Settlement SUCCESS
Outbox INSERT

COMMIT
```

异步：

```text
OutboxPublisher
       ↓
Kafka
```

Outbox：

```text
id
event_id
aggregate_type
aggregate_id

topic
event_type
payload

status
retry_count

created_at
published_at
```

状态：

```text
NEW
PUBLISHED
FAILED
```

---

# 十九、一个 Trade 的事务边界

一个 Trade 的资金变更推荐处于一个数据库事务。

伪代码：

```java
@Transactional
public void settle(TradeEvent trade) {

    createOrLockSettlementTask();

    validateTrade();

    ClearingResult clearing =
            clearingCalculator.calculate(trade);

    lockRelatedAccounts();

    applyBuyerSettlement();

    applySellerSettlement();

    applyFeeSettlement();

    createBalanceFlows();

    createJournal();

    updateSettlementSuccess();

    createOutboxEvent();
}
```

必须保证：

```text
余额变化成功
但是流水失败
```

不能发生。

```text
流水成功
但是余额变化失败
```

也不能发生。

---

# 二十、账户锁设计

一笔 Trade 至少可能涉及：

```text
Buyer Base Account
Buyer Quote Account
Seller Base Account
Seller Quote Account
Platform Fee Account
```

如果直接：

```text
SELECT ... FOR UPDATE
```

必须考虑数据库死锁。

统一规定锁顺序。

例如：

```text
按 account_id ASC 排序
```

后统一获取锁：

```text
Account A
Account B
Account C
Account D
```

任何 Settlement 都严格使用相同顺序。

避免：

```text
Transaction 1:
A → B

Transaction 2:
B → A
```

造成死锁。

同时实现数据库 Deadlock Retry。

---

# 二十一、不要依赖 Java synchronized

禁止：

```java
synchronized(userId)
```

作为资金并发安全方案。

因为 Settlement Service 将来会：

```text
Instance A
Instance B
Instance C
```

Java 本地锁不能解决分布式并发问题。

第一阶段依靠：

```text
Database Row Lock
+
Transaction
+
Unique Index
```

保证正确性。

---

# 二十二、余额更新

余额更新必须防止负数。

例如：

```sql
UPDATE account_balance
SET frozen = frozen - :amount
WHERE id = :id
  AND frozen >= :amount;
```

必须检查：

```text
affectedRows == 1
```

否则抛出：

```text
INSUFFICIENT_FROZEN_BALANCE
```

禁止：

```text
先 SELECT
再判断
再 UPDATE
```

作为唯一防线。

数据库写入本身必须具有资金保护能力。

---

# 二十三、异常分类

设计统一：

```text
SettlementException
```

错误分类：

```text
INVALID_TRADE

ACCOUNT_NOT_FOUND

INSUFFICIENT_AVAILABLE_BALANCE
INSUFFICIENT_FROZEN_BALANCE

DUPLICATE_TRADE

ACCOUNT_CONCURRENT_UPDATE

DATABASE_DEADLOCK

DATABASE_TIMEOUT

UNKNOWN_SETTLEMENT_ERROR
```

进一步分类：

```text
RETRYABLE
NON_RETRYABLE
MANUAL_REVIEW
```

例如：

```text
数据库超时
→ RETRY

Deadlock
→ RETRY

Kafka 重复消息
→ IGNORE

资金账不平
→ MANUAL_REVIEW
```

---

# 二十四、重试机制

禁止无限重试。

Settlement Task：

```text
retry_count
next_retry_time
last_error
```

例如：

```text
1s
5s
30s
2min
10min
```

超过阈值：

```text
MANUAL_REVIEW
```

并产生告警。

---

# 二十五、不要错误处理 Kafka Offset

不要设计：

```text
先提交 offset
再处理资金
```

必须保证：

资金结算事务成功以后才能认为消息处理成功。

如果：

```text
结算失败
```

Kafka 消息应该可以重试。

由于业务层有：

```text
tradeId UNIQUE
```

因此重复消费不会重复扣款。

---

# 二十六、Kafka Poison Message

对于永久失败消息：

```text
INVALID_DATA
ACCOUNT_DATA_CORRUPTED
```

不能无限阻塞 Partition。

设计：

```text
Retry Topic
DLQ
```

例如：

```text
cex.trade.executed

cex.trade.executed.retry

cex.trade.executed.dlq
```

但是涉及资金异常时：

不要单纯丢进 DLQ 后就当业务结束。

同时必须产生：

```text
Settlement MANUAL_REVIEW
+
Monitoring Alert
```

---

# 二十七、乱序问题

需要考虑：

```text
Trade T2
```

可能早于：

```text
Trade T1
```

到达 Settlement。

如果 Match Engine 已经通过：

```text
symbol partition
```

保证撮合顺序，则：

```text
matchSequence
```

必须被完整传递。

设计字段：

```text
symbol
matchSequence
```

Settlement 至少保留该 Sequence，用于：

* 排查
* 重放
* 对账
* 顺序验证

不要简单假设所有下游永久不会发生乱序。

---

# 二十八、同一用户多交易对并发

这是必须重点设计的场景。

例如用户同时交易：

```text
BTC-USDT
ETH-USDT
SOL-USDT
```

即使 Matching Engine 按 symbol 串行：

```text
BTC-USDT Partition
ETH-USDT Partition
SOL-USDT Partition
```

Settlement 层仍然可能同时修改：

```text
User.USDT
```

所以：

```text
Matching 单线程
```

并不能保证：

```text
Settlement 无资金并发
```

第一阶段必须通过：

```text
数据库账户行锁
```

解决。

---

# 二十九、Settlement Reconciliation 对账

必须开发独立的：

```text
Reconciliation Module
```

不要把资金系统正确性完全建立在“代码应该不会出错”上。

至少设计三类对账。

---

## 29.1 Trade vs Settlement

检查：

```text
Trade 已产生
```

但是：

```text
Settlement 不存在
```

SQL/任务逻辑：

```text
trade
LEFT JOIN settlement
```

找到：

```text
Missing Settlement
```

进行补偿。

---

## 29.2 Settlement vs Ledger

检查：

```text
Settlement SUCCESS
```

必须存在对应：

```text
Balance Flow
Journal
```

否则：

```text
DATA_INCONSISTENCY
```

---

## 29.3 Account vs Ledger

根据 Ledger 汇总：

```text
initial balance
+
all flows
```

应该等于：

```text
current balance
```

发现差异：

```text
reconciliation_difference
```

记录差异。

---

# 三十、Reconciliation Result

设计：

```text
reconciliation_result
```

字段：

```text
id

reconciliation_type

biz_id
trade_id
user_id
asset

expected_amount
actual_amount
difference

status

error_message

created_at
resolved_at
```

状态：

```text
OPEN
CONFIRMED
COMPENSATED
IGNORED
```

---

# 三十一、补偿机制

补偿不是简单：

```text
再执行一遍 UPDATE
```

所有补偿必须：

```text
有唯一 compensationId
有 Ledger
有 Balance Flow
有操作人/来源
有原因
有时间
```

补偿流水：

```text
biz_type = COMPENSATION
```

禁止直接 DBA 手工改余额且不留流水。

---

# 三十二、平台手续费账户

不能让手续费凭空消失。

设计平台账户，例如：

```text
SYSTEM_USER / PLATFORM_ACCOUNT
```

手续费：

```text
User asset -
Platform asset +
```

形成完整资金路径。

手续费数据必须可：

```text
查询
统计
对账
审计
```

---

# 三十三、禁止修改历史流水

以下表原则上 Append Only：

```text
balance_flow
journal
journal_entry
```

已经产生的历史资金流水禁止：

```text
UPDATE amount
DELETE
```

发现错误：

通过：

```text
reverse transaction
compensation transaction
```

进行修正。

---

# 三十四、订单取消和剩余资金释放

需要与订单取消流程正确配合。

订单：

```text
NEW
PARTIALLY_FILLED
```

取消时：

```text
CANCEL_REQUESTED
```

Matching Engine 真正确认：

```text
CANCELED
```

以后才能释放：

```text
remaining frozen balance
```

避免：

```text
订单还可能成交
资金已经提前解冻
```

发生资金穿透。

建议事件：

```text
order.cancel.confirmed
```

Settlement / Asset Service 消费后执行：

```text
unfreeze remaining amount
```

并生成：

```text
ORDER_UNFREEZE
```

流水。

---

# 三十五、Race Condition

重点测试：

```text
Trade
```

与：

```text
Cancel
```

同时发生。

必须保证：

如果订单最后一次成交与 Cancel 竞争：

```text
成交数量
+
取消数量
=
订单总数量
```

资金：

```text
已成交资金
+
已解冻资金
=
原始冻结资金
```

绝对不能：

```text
double settlement
double unfreeze
```

---

# 三十六、领域模块划分

推荐：

```text
settlement-service
│
├── application
│   ├── SettlementApplicationService
│   ├── TradeSettlementHandler
│   ├── OrderUnfreezeHandler
│   └── ReconciliationApplicationService
│
├── domain
│   ├── clearing
│   │   ├── ClearingCalculator
│   │   ├── ClearingResult
│   │   └── FeeCalculator
│   │
│   ├── settlement
│   │   ├── Settlement
│   │   ├── SettlementStatus
│   │   └── SettlementService
│   │
│   ├── account
│   │   ├── Account
│   │   ├── AccountBalance
│   │   └── Money
│   │
│   ├── ledger
│   │   ├── Journal
│   │   ├── JournalEntry
│   │   └── BalanceFlow
│   │
│   └── reconciliation
│
├── infrastructure
│   ├── persistence
│   ├── kafka
│   ├── outbox
│   ├── repository
│   └── lock
│
├── interfaces
│   ├── consumer
│   ├── controller
│   └── dto
│
└── scheduler
    ├── SettlementRetryJob
    ├── OutboxPublishJob
    └── ReconciliationJob
```

根据当前项目结构适当调整。

不要为了使用 DDD 而过度设计。

---

# 三十七、核心类

至少考虑：

```text
TradeExecutedConsumer

SettlementApplicationService

ClearingCalculator

FeeCalculator

SettlementTransactionService

AccountRepository

SettlementRepository

BalanceFlowRepository

JournalRepository

OutboxRepository

SettlementRetryService

ReconciliationService

SettlementRecoveryService
```

职责保持单一。

---

# 三十八、Money 类型

最好统一封装：

```java
Money
```

或者建立统一：

```text
AmountUtil
PrecisionUtil
```

禁止业务代码中随处：

```java
setScale(8)
setScale(18)
```

统一按照：

```text
Asset Precision
Price Precision
Quantity Precision
```

处理。

禁止：

```text
new BigDecimal(double)
```

必须：

```text
new BigDecimal(String)
```

或者直接由字符串反序列化为 BigDecimal。

---

# 三十九、数据库事务要求

检查：

```text
@Transactional
```

边界必须放在：

```text
Application Service
/
Transaction Service
```

不要出现：

```text
private method @Transactional
```

不起作用的情况。

不要：

```text
事务中执行远程 HTTP
事务中等待 Kafka
事务中执行长耗时逻辑
```

数据库事务只承担账务原子修改。

---

# 四十、接口设计

清算结算主要使用 Kafka，但仍提供后台查询接口。

例如：

```text
GET /api/settlements/{tradeId}

GET /api/settlements?userId=&status=

GET /api/ledger/flows

GET /api/reconciliation/issues
```

管理接口：

```text
POST /api/admin/settlements/{tradeId}/retry

POST /api/admin/reconciliation/{id}/recheck
```

补偿接口必须：

```text
严格权限控制
审计日志
禁止普通用户访问
```

不要允许任意 API 直接修改余额。

---

# 四十一、监控指标

加入 Micrometer / Prometheus Metrics。

至少：

```text
settlement_success_total

settlement_failed_total

settlement_retry_total

settlement_manual_review_total

settlement_latency

settlement_consumer_lag

settlement_db_deadlock_total

settlement_duplicate_trade_total

settlement_insufficient_balance_total

reconciliation_difference_total

outbox_pending_total

outbox_failed_total
```

---

# 四十二、日志

资金系统日志必须支持：

```text
traceId
eventId
tradeId
buyOrderId
sellOrderId
buyerUserId
sellerUserId
symbol
matchSequence
```

但是不要在普通日志大量打印：

```text
完整隐私数据
密钥
Token
```

---

# 四十三、测试要求

禁止只写 Happy Path。

必须覆盖：

## 正常测试

```text
BUY / SELL 正常成交

Maker / Taker

Buyer Fee

Seller Fee
```

---

## 部分成交

```text
Order
↓
Trade1
↓
Trade2
↓
Trade3
↓
FILLED
```

验证：

```text
余额
冻结
手续费
流水
订单状态
```

全部正确。

---

## Kafka 重复消息

同一个：

```text
tradeId
```

重复消费：

```text
100 次
```

最终资金只能变化：

```text
1 次
```

---

## Consumer Restart

模拟：

```text
账户 UPDATE 成功
事务 COMMIT
Kafka offset 未提交
程序宕机
```

服务启动以后消息重新消费。

最终：

```text
不能重复扣款
```

---

# 四十四、事务回滚测试

模拟：

```text
Buyer 修改成功

Seller 修改时异常
```

最终：

```text
Buyer 修改也必须回滚
```

不能存在：

```text
Buyer 成功
Seller 失败
```

的半结算状态。

---

# 四十五、Outbox 故障测试

模拟：

```text
Settlement DB COMMIT
Kafka Broker DOWN
```

要求：

```text
Outbox 保留 NEW
```

Kafka 恢复后：

```text
自动重新发送
```

最终：

```text
PUBLISHED
```

---

# 四十六、死锁测试

并发执行：

```text
Trade A
User1 ↔ User2

Trade B
User2 ↔ User1
```

验证：

```text
锁顺序
Deadlock Retry
```

不能产生资产错误。

---

# 四十七、并发测试

同一用户：

```text
BTC-USDT
ETH-USDT
SOL-USDT
```

同时大量成交。

验证：

```text
USDT Account
```

最终余额正确。

---

# 四十八、资金不变量测试

所有测试结束以后必须检查：

```text
available >= 0

frozen >= 0
```

以及：

```text
资产不能凭空产生
资产不能凭空消失
```

考虑平台手续费以后：

```text
用户资产总变化
+
平台账户变化
```

必须满足资金守恒规则。

---

# 四十九、Recovery

服务重新启动以后：

扫描：

```text
PROCESSING
```

并且：

```text
processing_at < NOW - timeout
```

的 Settlement。

进入：

```text
RETRY
```

重新处理。

因为：

```text
tradeId
```

具有唯一约束，所以恢复操作必须保持幂等。

---

# 五十、人工处理

设计：

```text
MANUAL_REVIEW
```

用于：

```text
账户数据异常
Ledger 不平
资金差异
未知历史错误
超过最大重试次数
```

但是禁止系统自动：

```text
忽略
```

资金异常。

---

# 五十一、安全红线

代码中禁止出现以下设计：

### 错误 1

```text
收到 Kafka
↓
先提交 offset
↓
修改资金
```

---

### 错误 2

```text
SELECT balance
↓
if enough
↓
UPDATE
```

且数据库 UPDATE 本身没有余额检查。

---

### 错误 3

使用：

```text
double
```

表示资金。

---

### 错误 4

仅使用 Redis 防重复扣款。

---

### 错误 5

Settlement 成功后直接 Kafka Send，不使用 Outbox。

---

### 错误 6

账户修改和流水插入不在同一事务。

---

### 错误 7

catch Exception：

```java
catch (Exception e) {
    log.error(...);
}
```

然后继续提交 Kafka Offset。

---

### 错误 8

资金异常只打 Log 不告警。

---

### 错误 9

后台提供：

```text
setBalance(user, amount)
```

接口。

---

### 错误 10

人工直接 UPDATE account 表不产生 Ledger。

---

# 五十二、开发阶段

不要一次性生成整个系统。

严格按照以下阶段开发。

---

## Phase 1：分析现有项目

首先扫描现有代码：

```text
订单结构
TradeEvent
Kafka Topic
Account 表
Order 表
Outbox
统一异常
数据库规范
Money 精度
模块结构
```

输出：

```text
现有能力
可以复用的模块
缺失能力
需要修改的地方
```

此阶段不要写大量代码。

---

## Phase 2：Domain Model + ER

设计：

```text
Settlement
ClearingResult
BalanceFlow
Journal
JournalEntry
ReconciliationResult
```

并给出：

```text
ER
DDL
唯一索引
普通索引
字段含义
```

重点评审：

```text
幂等
资金流水
事务边界
```

---

## Phase 3：Kafka Trade Consumer

开发：

```text
TradeExecutedConsumer
```

完成：

```text
反序列化
参数检查
幂等
Settlement Task
异常分类
Retry
DLQ
```

暂时不要直接修改资金。

---

## Phase 4：Clearing Calculator

实现：

```text
Buyer Clearing
Seller Clearing
Maker Fee
Taker Fee
Price
Quantity
QuoteAmount
```

必须全部单元测试。

---

## Phase 5：Settlement Transaction

开发核心事务。

完成：

```text
账户锁定
余额更新
冻结余额扣减
资产增加
手续费
平台账户
资金流水
Journal
Settlement SUCCESS
Outbox
```

这是整个项目最关键的阶段。

---

## Phase 6：Partial Fill + Unfreeze

实现：

```text
部分成交
完全成交
Cancel
剩余资金释放
Price Improvement
```

---

## Phase 7：Outbox

实现：

```text
trade.settled
```

以及：

```text
Outbox Publisher
Retry
Status
```

---

## Phase 8：Recovery

实现：

```text
PROCESSING Timeout Recovery

Settlement Retry

DLQ Handling
```

---

## Phase 9：Reconciliation

实现：

```text
Trade vs Settlement

Settlement vs Ledger

Account vs Ledger
```

---

## Phase 10：Admin / Observability

实现：

```text
Settlement Query

Ledger Query

Reconciliation Query

Manual Retry

Metrics

Alert
```

---

## Phase 11：Integration Test

完整跑通：

```text
Create Order
↓
Freeze
↓
Kafka
↓
Matching
↓
Trade
↓
Settlement
↓
Balance
↓
Ledger
↓
Outbox
↓
Order Update
```

---

# 五十三、验收场景

最终至少完整演示以下场景。

## Scenario A

```text
Buyer:
10000 USDT

Seller:
1 BTC
```

订单：

```text
BUY
0.1 BTC
100000 USDT

SELL
0.1 BTC
100000 USDT
```

完成：

```text
成交
结算
手续费
资金流水
订单完成
```

---

## Scenario B

```text
PARTIALLY_FILLED
```

连续多笔成交。

---

## Scenario C

Kafka 同一个 Trade：

```text
重复投递 10 次
```

最终只结算一次。

---

## Scenario D

结算事务过程中人为抛异常：

```text
完整 rollback
```

---

## Scenario E

DB Commit 后服务立即 Crash：

消息重新消费以后：

```text
资金没有重复变化
```

---

## Scenario F

Kafka Broker 故障：

Settlement：

```text
SUCCESS
```

Outbox：

```text
NEW
```

Kafka 恢复：

```text
自动发布
```

---

## Scenario G

同一用户多个 Symbol 并发成交。

---

## Scenario H

买单发生：

```text
Price Improvement
```

最终：

```text
成交金额正确
剩余资金正确解冻
```

---

# 五十四、代码质量要求

代码必须：

```text
可编译
可运行
有测试
有 migration
有 README
```

避免：

```text
TODO
伪代码
空实现
Mock 核心账务逻辑
```

所有资金修改逻辑都必须具备实际测试。

---

# 五十五、每个 Phase 完成后的输出格式

完成每个阶段以后暂停继续开发，并输出：

```text
## 本阶段完成内容

## 新增文件

## 修改文件

## 数据库变化

## Kafka 变化

## 核心设计说明

## 幂等策略

## 事务边界

## 风险点

## 测试结果

## 下一阶段计划
```

不要未经分析连续重构整个项目。

---

# 五十六、第一步现在执行

现在先执行：

# Phase 1：现有项目分析

请扫描当前工程代码，重点找到：

```text
Order
Trade
TradeEvent
Matching Engine
Kafka Topic
Account
Balance
Frozen Balance
Outbox
Database Migration
Common Exception
Money / BigDecimal
```

然后给我输出：

```text
1. 当前已有架构
2. 当前 TradeEvent 结构
3. 当前账户模型
4. 当前冻结资金实现
5. 当前 Kafka Topic
6. 当前 Outbox 实现
7. 哪些代码可以直接复用
8. 清算结算缺失模块
9. 需要增加的数据库表
10. 需要增加的 Kafka Topic
11. 推荐的模块目录
12. Phase 2 准备修改的文件
```

先不要开始 Phase 2。

完成 Phase 1 分析以后停止，等待下一条开发指令。
