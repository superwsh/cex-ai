# CEX 行情系统开发提示词

你是一名资深中心化交易所（CEX）架构师、Java 高并发工程师和行情系统专家。

当前项目已经完成：

- 用户与账户基础模块
- 订单系统 Order Service
- 撮合系统 Matching Engine
- 清算 / 结算系统 Clearing & Settlement

现在开始开发：

# Market Data System 行情系统

目标是构建一套接近真实中心化交易所生产环境的行情系统，支持：

- 实时成交
- 最新价格
- 24h Ticker
- Best Bid / Ask
- Order Book Depth
- Depth Snapshot
- Depth Incremental Update
- Sequence 连续性检测
- KLine / Candlestick
- REST 行情查询
- WebSocket 实时推送
- 历史行情
- Redis 缓存
- Kafka 消息消费
- 行情恢复
- 高并发 WebSocket
- 慢客户端处理
- 背压
- 行情压测
- 监控与告警

要求优先保证：

> 正确性 > 数据顺序 > 可恢复性 > 性能 > 可扩展性

---

# 一、技术栈

默认使用：

```text
Java 17+
Spring Boot 3.x

Kafka
Redis

MySQL 8.x

Netty / Spring WebFlux WebSocket

Micrometer
Prometheus
Grafana

Maven
Docker Compose
```

如果现有项目已经存在统一的技术栈、公共模块、异常体系、日志规范、Kafka 封装、Redis 封装，则必须优先复用现有实现，不允许重复造轮子。

不要为了所谓“架构先进”随意引入：

```text
Flink
Spark
ClickHouse
TimescaleDB
Cassandra
Pulsar
```

第一阶段保持架构简单。

只有当现有 MySQL / Redis / Kafka 明确无法满足需求时，再提出升级方案。

---

# 二、整体架构

设计以下数据链路：

```text
                   ┌────────────────────┐
                   │  Matching Engine   │
                   └─────────┬──────────┘
                             │
                    Trade / Book Event
                             │
                             ▼
                        ┌─────────┐
                        │  Kafka  │
                        └────┬────┘
                             │
              ┌──────────────┼──────────────┐
              ▼              ▼              ▼
        Trade Processor  Depth Processor  Ticker Processor
              │              │              │
              └──────────────┼──────────────┘
                             ▼
                    Market Data Core
                             │
               ┌─────────────┼──────────────┐
               ▼             ▼              ▼
             Redis          DB        WebSocket Publisher
                                              │
                                              ▼
                                      WebSocket Gateway
                                              │
                                              ▼
                                            Client
```

K线：

```text
TradeEvent
    │
    ▼
KLine Aggregator
    │
    ├── 当前周期内存聚合
    ├── Redis 缓存
    ├── Kafka / Async Persistence
    ▼
Historical KLine DB
```

必须保证：

1. 行情系统不能同步调用撮合系统获取实时行情。
2. 行情系统主要基于事件驱动构建。
3. 实时行情不能依赖频繁查询 MySQL。
4. Kafka Consumer 中不能存在大量数据库查询。
5. WebSocket Gateway 不负责复杂行情计算。
6. 行情计算和行情推送解耦。

---

# 三、模块划分

建议创建：

```text
market-data
├── market-data-domain
├── market-data-application
├── market-data-infrastructure
├── market-data-consumer
├── market-data-api
├── market-data-websocket
└── market-data-bootstrap
```

如果当前项目采用单体 Maven Module，可以使用：

```text
marketdata
├── domain
├── service
├── consumer
├── repository
├── cache
├── websocket
├── controller
├── model
├── config
└── metrics
```

职责必须清晰。

---

# 四、核心事件定义

首先检查撮合系统现有事件。

如果已经存在：

```text
TradeEvent
OrderBookChangedEvent
OrderMatchedEvent
OrderCanceledEvent
```

优先复用。

否则设计统一的行情事件。

---

# 五、TradeEvent

定义成交事件：

```java
TradeEvent {

    String eventId;

    long sequence;

    String symbol;

    String tradeId;

    String buyOrderId;

    String sellOrderId;

    String buyerUserId;

    String sellerUserId;

    long price;

    long quantity;

    long quoteQuantity;

    TradeSide takerSide;

    long tradeTime;

    long createdAt;
}
```

禁止核心行情计算使用：

```java
double
float
```

价格和数量优先使用整数定点数：

```text
price
quantity
```

例如：

```text
BTC precision = 8
USDT precision = 8
```

或者复用现有项目中的 Decimal / BigDecimal 规范。

必须保证：

```text
同一个 symbol 的 TradeEvent 顺序一致
```

Kafka 分区键：

```text
symbol
```

---

# 六、OrderBook 行情事件

设计：

```java
OrderBookDeltaEvent {

    String eventId;

    String symbol;

    long sequence;

    long previousSequence;

    List<PriceLevelChange> bids;

    List<PriceLevelChange> asks;

    long eventTime;
}
```

PriceLevelChange：

```java
PriceLevelChange {

    long price;

    long quantity;
}
```

约定：

```text
quantity > 0
更新当前价格档数量

quantity = 0
删除当前价格档
```

例如：

```json
{
  "symbol": "BTC-USDT",
  "sequence": 100002,
  "previousSequence": 100001,
  "bids": [
    ["6000000000000", "120000000"]
  ],
  "asks": [
    ["6000100000000", "0"]
  ]
}
```

---

# 七、Sequence 设计

Sequence 是整个行情系统的重要设计。

要求每个 symbol 独立维护：

```text
sequence
```

例如：

```text
BTC-USDT

10001
10002
10003
10004
```

消费端检查：

```text
previousSequence == localSequence
```

或者：

```text
incomingSequence == localSequence + 1
```

如果：

```text
local = 10001

incoming = 10003
```

说明：

```text
10002 丢失
```

此时：

1. 当前本地盘口标记为 INVALID。
2. 暂停向客户端发送该 symbol 的增量盘口。
3. 获取最新 Snapshot。
4. 重建 OrderBook。
5. 从对应 sequence 后继续消费增量消息。
6. 恢复 VALID。
7. 记录 sequence\_gap\_total 指标。

禁止：

```text
发现 sequence gap 后直接继续应用后续消息
```

---

# 八、Market Order Book

行情系统维护自己的只读盘口。

禁止直接读取 Matching Engine 内存中的订单簿对象。

建议：

```java
MarketOrderBook {

    NavigableMap<Long, Long> bids;

    NavigableMap<Long, Long> asks;

    long sequence;
}
```

排序：

```text
BID：

价格高 -> 低

ASK：

价格低 -> 高
```

只维护：

```text
price -> aggregatedQuantity
```

不需要存储每一笔订单。

例如：

```text
BUY

60000 -> 3 BTC
59999 -> 5 BTC
59998 -> 2 BTC
```

而不是保存：

```text
Order1
Order2
Order3
```

行情盘口属于 Level 2 聚合盘口。

---

# 九、Depth Snapshot

提供 REST：

```http
GET /api/v1/market/depth
```

参数：

```text
symbol
limit
```

例如：

```http
GET /api/v1/market/depth?symbol=BTC-USDT&limit=20
```

返回：

```json
{
  "symbol": "BTC-USDT",
  "sequence": 183729123,
  "bids": [
    ["60000.10", "1.20"],
    ["60000.00", "2.10"]
  ],
  "asks": [
    ["60001.00", "0.80"],
    ["60002.00", "3.40"]
  ],
  "timestamp": 1760000000000
}
```

支持：

```text
5
10
20
50
100
500
```

限制最大 Depth，避免用户通过：

```text
limit=1000000
```

造成服务压力。

---

# 十、Incremental Depth

WebSocket Channel：

```text
depth.{symbol}
```

例如：

```text
depth.BTCUSDT
```

推送：

```json
{
  "channel": "depth.BTCUSDT",
  "symbol": "BTCUSDT",
  "sequence": 183729124,
  "previousSequence": 183729123,
  "bids": [
    ["60000.10", "1.00"]
  ],
  "asks": [
    ["60001.00", "0"]
  ],
  "timestamp": 1760000000100
}
```

客户端标准恢复流程：

```text
1. 建立 WebSocket
2. 订阅 depth
3. 获取 REST Snapshot
4. 得到 snapshotSequence
5. 丢弃 <= snapshotSequence 的 delta
6. 从 snapshotSequence + 1 开始应用增量
7. 每次检查 sequence 连续性
8. 出现 gap 后重新获取 Snapshot
```

代码、接口文档和 README 必须说明该流程。

---

# 十一、Best Bid / Ask

维护：

```java
BookTicker {

    String symbol;

    long bidPrice;

    long bidQuantity;

    long askPrice;

    long askQuantity;

    long sequence;

    long timestamp;
}
```

WebSocket：

```text
bookTicker.BTCUSDT
```

只在最优买卖价发生变化时推送。

禁止每次整个盘口变化都发送 bookTicker。

---

# 十二、Trade Stream

提供：

```text
trade.BTCUSDT
```

消息：

```json
{
  "channel": "trade.BTCUSDT",
  "tradeId": "T123456",
  "symbol": "BTCUSDT",
  "price": "60000.10",
  "quantity": "0.12",
  "quoteQuantity": "7200.012",
  "side": "BUY",
  "timestamp": 1760000000000
}
```

REST：

```http
GET /api/v1/market/trades
```

支持最近：

```text
50
100
500
1000
```

笔成交。

---

# 十三、Ticker

实现：

```java
Ticker24h {

    String symbol;

    long lastPrice;

    long openPrice;

    long highPrice;

    long lowPrice;

    long priceChange;

    BigDecimal priceChangePercent;

    long volume;

    long quoteVolume;

    long tradeCount;

    long openTime;

    long closeTime;
}
```

REST：

```http
GET /api/v1/market/ticker/24hr
```

支持：

```text
单 symbol

GET /ticker/24hr?symbol=BTCUSDT
```

以及：

```text
全部 symbol

GET /ticker/24hr
```

WebSocket：

```text
ticker.BTCUSDT
```

---

# 十四、24h Rolling Window

注意：

```text
24h Ticker
```

不是：

```text
今天 00:00 ~ 当前
```

而是真正的：

```text
currentTime - 24h
~
currentTime
```

设计 Rolling Window。

可以使用：

```text
分钟级 bucket
```

例如维护：

```java
TickerMinuteBucket {

    long minute;

    long open;

    long high;

    long low;

    long close;

    long volume;

    long quoteVolume;

    long tradeCount;
}
```

保存最近：

```text
1440 个 minute bucket
```

利用滑动窗口快速计算过去 24h。

不要每次调用 ticker API 都执行：

```sql
SELECT ...
FROM trade
WHERE created_at > now() - 24 hour
```

---

# 十五、KLine

支持：

```text
1s
1m
3m
5m
15m
30m
1h
2h
4h
6h
12h
1d
1w
```

第一阶段至少实现：

```text
1m
5m
15m
1h
4h
1d
```

数据：

```java
KLine {

    String symbol;

    String interval;

    long openTime;

    long closeTime;

    long open;

    long high;

    long low;

    long close;

    long volume;

    long quoteVolume;

    long tradeCount;

    boolean closed;
}
```

---

# 十六、KLine 聚合原则

TradeEvent 到达：

```text
price = P
quantity = Q
```

当前 Candle：

```text
open：

第一笔成交价格

high：

max(high, P)

low：

min(low, P)

close：

P

volume：

volume + Q

quoteVolume：

quoteVolume + P * Q

tradeCount：

tradeCount + 1
```

周期结束：

```text
closed = true
```

然后：

1. 推送最终 KLine。
2. 持久化。
3. 创建下一个 Candle。

---

# 十七、KLine 时间窗口

必须统一按照：

```text
UTC Epoch
```

切分窗口。

例如 1m：

```text
openTime =
timestamp / 60000 * 60000
```

closeTime：

```text
openTime + 60000 - 1
```

禁止根据服务器本地时区计算。

---

# 十八、多周期 KLine

不要为：

```text
1m
5m
15m
1h
4h
1d
```

重复消费 6 次 TradeEvent。

推荐：

第一版允许直接：

```text
TradeEvent
  ↓
多个 Interval Aggregator
```

如果后续性能出现瓶颈，可以：

```text
Trade
 ↓
1m Candle
 ↓
聚合 5m / 15m / 1h
```

暂时不要过度优化。

---

# 十九、历史 KLine

接口：

```http
GET /api/v1/market/klines
```

参数：

```text
symbol
interval
startTime
endTime
limit
```

例如：

```http
GET /api/v1/market/klines
    ?symbol=BTCUSDT
    &interval=1m
    &limit=500
```

最大：

```text
1000
```

需要适当索引。

---

# 二十、数据库设计

设计：

```sql
market_kline
```

字段：

```text
id

symbol

interval

open_time
close_time

open_price
high_price
low_price
close_price

volume
quote_volume

trade_count

created_at
updated_at
```

唯一索引：

```text
UNIQUE(symbol, interval, open_time)
```

查询索引：

```text
INDEX(symbol, interval, open_time)
```

---

# 二十一、Trade History

如果撮合 / 清算系统已经有：

```text
trade
```

交易表，不重复创建完整成交事实表。

行情模块可以：

```text
复用 Trade 查询服务

或者

建立 market_recent_trade
```

仅用于高性能行情查询。

优先避免跨模块强数据库耦合。

---

# 二十二、Redis Key 设计

例如：

```text
market:ticker:BTCUSDT

market:bookTicker:BTCUSDT

market:depth:snapshot:BTCUSDT

market:kline:BTCUSDT:1m

market:trades:BTCUSDT
```

不要把 Redis 当永久数据库。

Redis 主要用于：

```text
热点行情
Snapshot
Ticker
最近成交
当前 KLine
```

---

# 二十三、Kafka Topic

根据现有系统统一命名规范。

建议逻辑上包含：

```text
matching.trade
matching.book.delta

market.trade
market.depth
market.ticker
market.kline

market.websocket
```

但不要为了模块划分创建过多 Topic。

第一版建议：

```text
cex.matching.trade
cex.matching.book-delta
cex.market.public
```

partition key：

```text
symbol
```

确保：

```text
同 symbol 顺序
```

---

# 二十四、Kafka Consumer

实现：

```text
TradeConsumer

OrderBookDeltaConsumer
```

要求：

1. 手动或可靠 Offset 管理。
2. 消费逻辑必须幂等。
3. 禁止阻塞 Kafka Consumer Thread。
4. 避免消费时同步调用远程服务。
5. 异常必须分类。
6. 对不可恢复数据错误发送 DLQ 或告警。
7. Sequence 错误不能仅靠 Kafka Retry 解决。

---

# 二十五、幂等

TradeEvent：

```text
tradeId
```

作为业务唯一标识。

如果存在：

```text
eventId
```

可以使用：

```text
eventId
```

进行事件幂等。

KLine 数据库使用：

```text
symbol + interval + openTime
```

唯一索引防止重复插入。

---

# 二十六、WebSocket Gateway

WebSocket Gateway 独立承担：

```text
Connection Management
Subscription Management
Heartbeat
Authentication（私有频道时）
Rate Limit
Fan-Out
Backpressure
Slow Consumer Handling
```

行情公共频道不要求登录。

---

# 二十七、WebSocket 协议

客户端：

```json
{
  "method": "SUBSCRIBE",
  "params": [
    "ticker.BTCUSDT",
    "depth.BTCUSDT",
    "trade.BTCUSDT",
    "kline.1m.BTCUSDT"
  ],
  "id": 1
}
```

服务端：

```json
{
  "result": null,
  "id": 1
}
```

取消：

```json
{
  "method": "UNSUBSCRIBE",
  "params": [
    "depth.BTCUSDT"
  ],
  "id": 2
}
```

---

# 二十八、Subscription Manager

不要：

```text
每连接创建一个 Kafka Consumer
```

必须：

```text
Kafka
  ↓
一个内部行情 Consumer
  ↓
Channel
  ↓
大量 WebSocket Session
```

维护：

```text
channel -> sessions
```

例如：

```java
Map<String, Set<Session>>
```

实际实现要考虑高并发安全。

---

# 二十九、WebSocket Fan-Out

例如：

```text
BTCUSDT Depth

1 条行情
      ↓
50000 个连接订阅
```

不能：

```text
序列化 50000 次
```

应该：

```text
行情对象
 ↓
序列化一次
 ↓
ByteBuf / byte[]
 ↓
广播
```

尽量复用序列化结果。

---

# 三十、慢客户端

必须设计：

```text
Slow Consumer Protection
```

如果某客户端：

```text
网络速度过慢
```

导致：

```text
send queue
```

持续积压。

需要设置：

```text
MAX_PENDING_MESSAGES

MAX_PENDING_BYTES
```

超过阈值：

```text
主动关闭连接
```

不能因为一个客户端拖慢所有用户。

---

# 三十一、背压

针对高频：

```text
Depth
Ticker
```

允许采用：

```text
Conflation
```

例如客户端处理不过来：

Ticker：

```text
100
101
102
103
```

可以只保留：

```text
103
```

但是：

```text
Trade Stream
```

如果要求逐笔成交，则不能随意丢弃。

Depth 如果丢增量：

```text
必须让客户端重新 Snapshot
```

---

# 三十二、行情推送频率

不要所有行情变化都直接广播。

设计：

```text
Trade：

实时

BookTicker：

变化时

Depth：

10ms / 50ms / 100ms 批量推送

Ticker：

100ms / 500ms / 1s

KLine：

500ms / 1s 更新当前 Candle
```

具体频率做成配置。

---

# 三十三、Batch / Conflation

对于同一个：

```text
symbol
```

100ms 内产生：

```text
100 次 OrderBook Delta
```

可以聚合：

```text
price=60000:

1 -> 2 -> 4 -> 3
```

最终只发送：

```text
60000 -> 3
```

但是必须正确处理：

```text
firstSequence
lastSequence
```

客户端才能检测连续性。

---

# 三十四、Snapshot 恢复

行情服务重启：

方案一：

```text
Matching Engine Snapshot
+
Kafka Replay
```

方案二：

```text
Market Data Snapshot
+
Kafka Replay
```

第一阶段建议行情模块自己定期保存：

```text
OrderBook Snapshot
sequence
```

恢复：

```text
1. 加载最新 Snapshot
2. 得到 sequence = N
3. Kafka 从对应 offset / event sequence 恢复
4. 重放 N 之后事件
5. 完成后开放行情服务
```

如果现有 Kafka 事件无法根据 sequence 对应 offset，需要设计恢复索引或直接请求 Matching Engine 最新 Snapshot。

---

# 三十五、行情状态

每个 symbol 维护：

```java
MarketDataStatus {

    INIT,

    RECOVERING,

    ACTIVE,

    INVALID,

    SUSPENDED
}
```

只有：

```text
ACTIVE
```

才允许正常推送盘口行情。

出现 sequence gap：

```text
ACTIVE
 ↓
INVALID
 ↓
RECOVERING
 ↓
ACTIVE
```

---

# 三十六、交易对状态

支持：

```text
PRE_TRADING

TRADING

HALT

POST_TRADING

DELISTED
```

如果：

```text
HALT
```

停止撮合后：

行情系统仍然允许读取历史行情和最后盘口。

---

# 三十七、REST API

至少实现：

```text
GET /api/v1/market/symbols

GET /api/v1/market/ticker/24hr

GET /api/v1/market/bookTicker

GET /api/v1/market/depth

GET /api/v1/market/trades

GET /api/v1/market/klines

GET /api/v1/market/time
```

---

# 三十八、接口性能目标

第一阶段目标：

```text
Ticker API：

P99 < 30ms

Depth API：

P99 < 50ms

KLine 热数据：

P99 < 50ms
```

禁止 API 每次现算复杂行情。

读取路径优先：

```text
Memory
 ↓
Redis
 ↓
Database
```

---

# 三十九、监控指标

增加：

```text
market_trade_events_total

market_depth_events_total

market_sequence_gap_total

market_event_delay_ms

market_kline_delay_ms

market_ws_connections

market_ws_subscriptions

market_ws_messages_total

market_ws_send_queue_size

market_ws_slow_consumer_total

market_kafka_consumer_lag

market_snapshot_age_seconds
```

---

# 四十、日志

重要日志必须包含：

```text
symbol
sequence
tradeId
eventId
partition
offset
```

Sequence Gap：

```text
ERROR
```

必须明确打印：

```text
symbol
localSequence
incomingPreviousSequence
incomingSequence
partition
offset
```

---

# 四十一、异常处理

分类：

```text
BusinessException

MarketSequenceException

InvalidMarketEventException

MarketDataRecoveryException

WebSocketProtocolException
```

不能所有异常统一：

```text
catch(Exception e)
```

然后继续执行。

---

# 四十二、测试

必须实现 Unit Test：

```text
Ticker calculation

24h rolling window

OrderBook delta

Best Bid / Ask

Depth snapshot

Sequence gap

KLine open

KLine high

KLine low

KLine close

KLine volume

KLine rollover

Duplicate event
```

---

# 四十三、盘口测试

例如：

初始：

```text
BUY

100 -> 10
99  -> 20

SELL

101 -> 15
102 -> 30
```

收到：

```text
BUY 100 -> 5
BUY 98 -> 10

SELL 101 -> 0
SELL 103 -> 20
```

最终：

```text
BUY

100 -> 5
99  -> 20
98  -> 10

SELL

102 -> 30
103 -> 20
```

验证：

```text
bestBid = 100

bestAsk = 102
```

---

# 四十四、KLine 测试

1m：

```text
10:00:00 price=100 qty=1
10:00:10 price=105 qty=2
10:00:20 price=98  qty=3
10:00:59 price=102 qty=1
```

必须得到：

```text
open = 100
high = 105
low = 98
close = 102
volume = 7
tradeCount = 4
```

---

# 四十五、Sequence 测试

正常：

```text
100
101
102
103
```

异常：

```text
100
101
103
```

要求：

```text
检测缺失 102

MarketStatus -> INVALID

触发恢复

禁止继续正常发布盘口
```

---

# 四十六、WebSocket 测试

覆盖：

```text
连接

断开

重复订阅

取消订阅

非法 channel

不存在 symbol

心跳

超时

大量订阅

慢客户端

广播

重连
```

---

# 四十七、压测

设计行情压测程序。

场景：

### 场景 1

```text
100 symbol
10000 trade / second
```

验证：

```text
行情延迟
CPU
GC
Kafka Lag
```

### 场景 2

```text
100000 WebSocket Connections
```

每连接：

```text
1~5 subscription
```

验证：

```text
Memory
CPU
Network
GC
Send Queue
```

### 场景 3

热门交易对：

```text
BTCUSDT

50000 subscribers
```

持续：

```text
1000 depth updates/s
```

验证广播能力。

---

# 四十八、禁止事项

禁止：

```text
1. 每次 Ticker 请求查询交易表计算 24h 数据。

2. 每次 Depth 请求查询订单表。

3. WebSocket Session 直接订阅 Kafka。

4. 一个用户一个 Kafka Consumer。

5. 行情系统同步 RPC 调用撮合系统获取每次盘口变化。

6. 使用 double 处理金额和价格。

7. Sequence Gap 后继续更新盘口。

8. Kafka Consumer 中执行大量慢 SQL。

9. WebSocket 无限制缓存待发送消息。

10. 每个客户端重复 JSON Serialize。

11. Redis 作为唯一真实数据源。

12. Kafka 消息没有 version。

13. Event DTO 和数据库 Entity 共用。

14. Controller 编写核心行情逻辑。

15. 在 WebSocket Handler 中计算 KLine。
```

---

# 四十九、Event Version

所有跨系统 Event 建议包含：

```java
String eventType;

int version;

String eventId;

long timestamp;
```

例如：

```json
{
  "eventType": "TRADE",
  "version": 1,
  "eventId": "...",
  "timestamp": 1760000000000
}
```

为以后协议升级预留能力。

---

# 五十、开发顺序

严格按照以下阶段执行。

不要一次性实现整个行情系统。

---

# Phase 1：事件协议

完成：

```text
TradeEvent

OrderBookDeltaEvent

PriceLevelChange

Event Version

Kafka Topic

Sequence 规则
```

输出：

```text
Java DTO
Topic 定义
序列化方案
协议文档
```

完成后停止。

---

# Phase 2：Market Order Book

实现：

```text
MarketOrderBook

Bid Tree

Ask Tree

Apply Delta

Sequence Check

BestBid

BestAsk

Depth Snapshot
```

完成单元测试。

暂时：

```text
不开发 WebSocket
不开发 KLine
```

---

# Phase 3：Trade + Ticker

实现：

```text
Trade Consumer

Recent Trades

Last Price

BookTicker

24h Ticker

Rolling Window
```

加入 Redis。

完成后测试。

---

# Phase 4：KLine

实现：

```text
KLine Aggregator

1m
5m
15m
1h
4h
1d

Current Candle
Close Candle
Persistence
Redis Cache
```

完成：

```text
KLine REST API
```

---

# Phase 5：REST Market API

实现：

```text
Symbols

Ticker

BookTicker

Depth

Trades

KLines

Server Time
```

统一：

```text
Response DTO
Error Code
Validation
Rate Limit
```

---

# Phase 6：WebSocket

实现：

```text
Connection Manager

Subscription Manager

Heartbeat

SUBSCRIBE

UNSUBSCRIBE

Trade Channel

Ticker Channel

BookTicker Channel

Depth Channel

KLine Channel
```

---

# Phase 7：行情广播优化

实现：

```text
Batch

Conflation

Serialize Once

Fan-Out

Backpressure

Slow Consumer Protection
```

---

# Phase 8：Snapshot & Recovery

实现：

```text
Market Snapshot

Sequence Persistence

Startup Recovery

Sequence Gap Recovery

Kafka Replay

Market Status Machine
```

---

# Phase 9：监控

加入：

```text
Metrics

Prometheus

Kafka Lag

WS Connections

Sequence Gap

Event Delay

Slow Consumer
```

---

# Phase 10：压测

开发：

```text
Market Event Generator

WebSocket Load Tester

Kafka Throughput Test

Depth Load Test
```

输出：

```text
TPS

P50
P95
P99

CPU
Memory
GC

Kafka Lag

WS Connection Count

Outbound Bandwidth
```

---

# 五十一、Codex 工作规则

在每一个 Phase 开始前：

第一步：

```text
阅读当前项目代码
```

确认：

```text
现有模块
代码规范
包结构
Kafka 封装
Redis 封装
异常处理
公共 DTO
日志规范
数据库规范
测试规范
```

第二步：

输出：

```text
当前代码分析
需要修改的文
件
需要新增的文件
数据库变更
Kafka 变更
风险点
```

第三步：

才开始编码。

禁止直接创建大量代码。

---

# 五十二、每阶段完成后的固定输出

每完成一个 Phase，都必须输出：

## 1. 本阶段实现内容

说明完成了什么。

## 2. 修改文件

例如：

```text
新增：

TradeEvent.java
MarketOrderBook.java

修改：

KafkaConfig.java
application.yml
```

## 3. 核心设计

说明重要设计决策。

## 4. 测试

输出：

```text
Unit Tests
Integration Tests
```

以及测试结果。

## 5. 尚未实现

明确列出：

```text
下一阶段才实现的功能
```

禁止偷偷提前开发后面的模块。

## 6. 风险

说明当前可能存在的：

```text
性能
一致性
并发
数据恢复
兼容性
```

问题。

---

# 五十三、代码质量要求

所有代码必须：

```text
可编译
可运行
可测试
```

禁止：

```java
TODO
```

代替关键逻辑。

禁止：

```java
return null;
```

作为未完成实现。

禁止生成：

```text
Demo Code
Pseudo Code
Fake Implementation
```

核心业务必须真实实现。

---

# 五十四、最终目标

最终形成：

```text
                    Matching Engine
                          │
           ┌──────────────┴──────────────┐
           │                             │
       TradeEvent                  BookDeltaEvent
           │                             │
           └──────────── Kafka ──────────┘
                         │
                         ▼
                 Market Data Core
                         │
          ┌──────────────┼──────────────┐
          │              │              │
        Trades         Ticker          Depth
          │              │              │
          └──────────┬───┴──────┬──────┘
                     │          │
                   KLine       Redis
                     │
                     ▼
                    DB
                     │
          ┌──────────┴──────────┐
          ▼                     ▼
       REST API           WebSocket Gateway
                                  │
                       ┌──────────┼──────────┐
                       ▼          ▼          ▼
                     Trade      Depth      KLine
                       │          │          │
                       └──────── Client ─────┘
```

系统最终必须支持：

```text
实时成交
实时盘口
盘口快照
盘口增量
Sequence 连续性
Best Bid / Ask
24h Ticker
KLine
REST
WebSocket
行情缓存
历史查询
宕机恢复
行情重放
慢客户端隔离
背压
高并发广播
监控
压测
```

---

# 五十五、现在开始执行

现在只执行：

# Phase 1：行情事件协议与基础模型设计

不要直接开发整个行情系统。

首先扫描并理解现有：

```text
订单模块
撮合模块
清算结算模块
Kafka 配置
Trade Event
Order Book
Symbol 配置
价格精度
数量精度
公共模块
```

然后输出：

```text
1. 当前项目分析

2. 行情系统模块结构

3. TradeEvent 最终定义

4. OrderBookDeltaEvent 最终定义

5. Sequence 生成与连续性方案

6. Kafka Topic 方案

7. Kafka Partition Key 方案

8. Event Version 方案

9. 新增 / 修改文件清单

10. Phase 1 开发计划
```

确认设计没有明显问题后，直接完成 Phase 1 的代码、测试和文档。

完成 Phase 1 后停止，不要继续 Phase 2。
